to start
from /invetory run:
uvicorn main:app --reload --port=8000

from /payment run:
uvicorn main:app --reload --port=8001

send post on /products
{
    "name": "product",
    "price": 20,
    "quantity": 100
}